#include "gui.c"
#include "messenger.c"
#include <stdbool.h>
#include <ctype.h>

int main()
{
  // init_gui();
  // printf("Enter the number of the color and press enter: ");
  // int input = 0;
  // scanf("%d", &input);
  // printf("\n");
  // printf("%d selected\n", input);
  messenger();
  return 0;
}