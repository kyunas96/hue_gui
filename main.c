#include "messenger.c"
#include <stdbool.h>
#include <ctype.h>
#include <curl/curl.h>

int main()
{
  messenger();
  // init_gui();
  // printf("Enter the number of the color and press enter: ");
  // int input = 0;
  // scanf("%d", &input);
  // printf("\n");
  // printf("%d selected\n", input);
  return 0;
}